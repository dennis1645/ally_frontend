/**
 * Dedicated authenticated route for Assessment 2 / Deep Diagnostic.
 *
 * Keep this separate from INITIAL_ASSESSMENT_ROUTE so the Research
 * Trail never sends an existing user back into the public onboarding
 * diagnostic flow.
 */
export const ASSESSMENT_2_ROUTE =
  "/assessment/deep-diagnostic";